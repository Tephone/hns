<%= form_with(model: @horse_diary, local: true, url: horse_diary_new_or_edit_path?) do |f|%>
  <div class = "field">
    <%= f.label :horse_diary %><br>
    <%= f.text_area :content %>
  </div>
  <div>
    <%= f.submit%>
  </div>
<% end %>